{
  "title": "Refinery — HTML for RAG / LLM",
  "type": "object",
  "schemaVersion": 1,
  "description": "Clean HTML for RAG and LLM pipelines. Paste HTML or pass URLs. Strips scripts, CSS, and layout noise. Returns text + word_count for token budgeting.",
  "properties": {
    "urls": {
      "title": "URLs to clean",
      "type": "array",
      "description": "Fetch open URLs (example.com, httpbin.org/html). Live lipstickalley.com is Cloudflare-blocked — use paste demo below or Restore example input.",
      "items": {
        "type": "string"
      },
      "editor": "stringList",
      "prefill": ["https://example.com"],
      "sectionCaption": "Quick try — URL mode (open sites)"
    },
    "raw_payload": {
      "title": "HTML to clean (paste)",
      "type": "string",
      "description": "Paste raw HTML from your crawler. For the Lipstick Alley demo prefill in Console, run: python3 scripts/sync_console_source.py",
      "editor": "textarea",
      "prefill": "",
      "sectionCaption": "Quick try — heavy forum HTML (Lipstick Alley snapshot)"
    },
    "removeScripts": {
      "title": "Remove scripts",
      "type": "boolean",
      "description": "Strip JavaScript and tracking scripts",
      "default": true,
      "sectionCaption": "Options"
    },
    "removeStyles": {
      "title": "Remove styles",
      "type": "boolean",
      "description": "Strip CSS and style tags",
      "default": true
    },
    "includeMetadata": {
      "title": "Include metadata",
      "type": "boolean",
      "description": "Return language, word_count, and processing_time_ms",
      "default": true
    },
    "extractMentions": {
      "title": "Extract @mentions",
      "type": "boolean",
      "description": "Pull @mentions from social HTML",
      "default": false,
      "sectionCaption": "Advanced"
    },
    "extractHashtags": {
      "title": "Extract #hashtags",
      "type": "boolean",
      "description": "Pull hashtags from social HTML",
      "default": false
    }
  },
  "required": []
}
